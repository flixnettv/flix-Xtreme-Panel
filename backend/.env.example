# ==========================================
# Xtreme Panel - Environment Variables
# انسخ هذا الملف وسمّه .env وعدّل القيم
# ==========================================

# --- إعدادات السيرفر ---
# البورت اللي هيشتغل عليه التطبيق
PORT=5000

# --- إعدادات قاعدة البيانات ---
# عنوان السيرفر (localhost لو القاعدة على نفس الجهاز)
DB_HOST=localhost
# منفذ MySQL (افتراضي 3306)
DB_PORT=3306
# اسم المستخدم للقاعدة
DB_USER=xtreme_user
# الباسوورد (غيّره لقيمة قوية جداً)
DB_PASSWORD=StrongPassword123!
# اسم قاعدة البيانات
DB_NAME=xtreme_panel

# --- إعدادات JWT للمصادقة ---
# مفتاح سري لتوقيع التوكنز (غيّره لقيمة عشوائية طويلة)
JWT_SECRET=SuperSecretKey_2026_ChangeThisImmediately!
# مدة صلاحية التوكن (24 ساعة)
JWT_EXPIRES_IN=24h

# --- إعدادات البيئة ---
# NODE_ENV=development للتطوير، أو production للنشر
NODE_ENV=development

# --- إعدادات النسخ الاحتياطي (للسكربتات) ---
# مسار مجلد النسخ الاحتياطي
BACKUP_DIR=/var/backups/xtreme_panel
# عدد أيام الاحتفاظ بالنسخ القديمة
BACKUP_RETENTION_DAYS=7